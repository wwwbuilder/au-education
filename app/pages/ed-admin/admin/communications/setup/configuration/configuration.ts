import {Component} from '@angular/core';
import { ROUTER_DIRECTIVES } from '@angular/router';

@Component({
	moduleId: module.id,
	selector : 'setup-configuration',
  templateUrl : 'configuration.html',
	directives: [ROUTER_DIRECTIVES]
})

export class ConfigurationComponent {}
